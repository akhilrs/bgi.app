<!DOCTYPE html>
<html>
<head>
  <title><%= title %></title>
  <%= render 'layouts/stylesheets' %>
  <%= csrf_meta_tags %>
  <meta charset=”utf-8”> 
</head>
<body>
	<div class="container">
		<%= render 'layouts/header' %>
		<section class="round">
			<%= yield %>
		</section>
		<%= render 'layouts/footer' %>
	</div>
	<%= render 'layouts/scripts' %>
</body>
</html>
